2020/9/15
基线条件
调用栈
基准值
散列表适用于：仿真映射关系；防止重复；缓存/记住数据，以免服务器在通过处理来生成他们。
RSA算法：
散列表包含的元素除以位置总数，就是合理得填装因子
可以结合散列函数和数组来创建散列表。
散列冲突十分影响性能，可以用最大限度减少冲突的额外那里函数。
散列表的查找，插入和删除速度都非常快。
散列表适合用于仿真映射关系
一旦填充因子超过0.7，就该调整散列表的长度。
散列表可以用于缓存数据（例如，在web服务器上）
散列表非常适合用于防止重复。


广度优先搜索------用到队列